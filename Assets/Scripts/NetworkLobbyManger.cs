using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/* 
The class NetworkLobbyMannager is Class which is for the lobby 
to define the amount of player and host und guest.  
*/
public class NetworkLobbyManger : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
